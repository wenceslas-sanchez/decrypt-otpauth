[project]
name = "decrypt-otpauth"
version = "0.1.0"
description = "TODO"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cryptography==45.0.7",
    "pillow==11.3.0",
    "qrcode==8.2",
]

[dependency-groups]
lint = [
    "pre-commit==4.3.0",
    "ruff==0.12.10",
]
dev = [
    "pytest==8.4.1",
]
