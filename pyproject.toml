[project]
name = "decrypt-otpauth"
version = "0.1.0"
description = "TODO"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cryptography==46.0.1",
    "pillow==11.3.0",
    "qrcode==8.2",
]

[dependency-groups]
lint = [
    "pre-commit==4.3.0",
    "ruff==0.13.0",
]
dev = [
    "pytest==8.4.2",
]
